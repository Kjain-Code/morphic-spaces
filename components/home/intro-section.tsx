import { Section } from "@/components/ui/section";

export function IntroSection() {
  return (
    <Section eyebrow="Studio Intro">
      <p className="max-w-2xl border border-dashed border-[var(--ink-20)] bg-[var(--ink-faint)] px-6 py-8 font-serif text-xl font-light leading-relaxed text-[var(--ink)] sm:text-2xl">
        Content will be inserted from client.
      </p>
    </Section>
  );
}
